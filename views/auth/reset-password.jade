h3 Forgot password
form(name='form', novalidate, ng-submit='reset()')
  .row
    .input-wrapper(ng-class='{error: form.username.$dirty && !form.username.$valid}')
      label User name
        input(type='text', ng-model='user.username', id='username', name='username', placeholder='User name' ng-pattern='/^\\w*$/')
      small.error(ng-show='form.username.$dirty && form.username.$error.pattern')
        |User name contains illegal characters
  .row.text-center
    |Or
  .row
    .input-wrapper(ng-class='{error: form.email.$dirty && !form.email.$valid}')
      label Email
        input(type='email', id='email', name='email', placeholder='Email')
      small.error(ng-show='form.email.$dirty && form.email.$error.pattern')
        |Invalid email address
  .row
    small.error(ng-show='form.$dirty && !user.username && !user.email') Provide user name or email
    input(type='submit', value='Reset password').button.small.radius